# Railway deploy

This zip is fixed for Railway.

Settings:

- Root Directory: empty
- Build Command: npm run build
- Start Command: npm run start:safe
- Healthcheck Path: /api/health

Important: upload/push the CONTENTS of this folder as the repository root. Do not keep an extra parent folder above package.json.
