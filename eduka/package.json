{
  "name": "eduka",
  "version": "1.0.0",
  "private": true,
  "description": "Eduka landing frontend and backend service for eduka.uz",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "node backend/server.js",
    "seed": "node backend/seed.js",
    "build": "node --check backend/server.js && node --check frontend/crm-mock.js && node --check frontend/crm-services.js && node --check frontend/app.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=18"
  }
}
