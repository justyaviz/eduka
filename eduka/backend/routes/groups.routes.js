"use strict";
// Eduka 21.7.0 route module scaffold for groups.
// The current monolith still serves production endpoints from backend/server.js.
// Next refactor step: move the matching handlers here without changing API URLs.
function register(app) {
  return app;
}
module.exports = { register };
