# Fix Report 30.0.0

CRM core modules were consolidated and stabilized for production-style center admin workflows. The update adds a dedicated `/admin/crm-core-pro` interface and safe backend endpoints for student, teacher, group, payment, attendance, dashboard, search and export operations.

If Railway reports missing table/column issues, run:

`backend/hotfix-30.0.0-crm-core-pro.sql`
