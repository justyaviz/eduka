# FIX REPORT 32.0.0

This build makes premium CRM modules the primary admin experience and adds safe backend endpoints for CRM workflow, lead pipeline, reports/export, receipt/cancel flow, automation readiness and launch polish.

Run hotfix SQL if Railway database misses new columns/tables.
