{
  "name": "eduka-landing-2",
  "version": "2.0.0",
  "private": true,
  "description": "EDUKA 2.0 landing page rebuilt from scratch with Railway healthcheck support.",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node server.js",
    "start:safe": "node server.js",
    "dev": "node server.js",
    "doctor": "npm run build && node scripts/doctor.js"
  },
  "engines": {
    "node": ">=18"
  }
}
