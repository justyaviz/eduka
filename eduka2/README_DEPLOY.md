# Eduka Landing Exact UI

Railway settings:

- Root Directory: empty
- Build Command: `npm run build`
- Start Command: `npm run start:safe`
- Healthcheck Path: `/api/health`

Local test:

```bash
npm install
npm run build
npm run start:safe
```

Open:
- `/`
- `/uz`
- `/api/health`
