const fail=(m,s=403)=>Object.assign(new Error(m),{status:s});
async function checkLimit(db,center,entity,current,data,action){if(!['students','branches'].includes(entity)||action==='archive'||current&&!current.deleted&&current.data.status!=='Arxiv')return;if(data.status==='Arxiv')return;const row=(await db.query('SELECT t.student_limit,t.branch_limit FROM centers c JOIN tariffs t ON t.name=c.tariff WHERE c.id=$1',[center])).rows[0];const limit=Number(entity==='students'?row?.student_limit:row?.branch_limit);if(!limit||limit<0)return;const count=(await db.query("SELECT COUNT(*)::int n FROM eduka_records WHERE center_id=$1 AND entity=$2 AND deleted=0 AND COALESCE(data->>'status','Faol')<>'Arxiv'",[center,entity])).rows[0].n;if(count>=limit)throw fail('Tarif limiti tugadi. Tarifni oshiring.',409)}
module.exports={checkLimit};
